# Selenium-automation
This project contains a Selenium-based web automation script that interacts with the SailTenders website (sailtenders.co.in). The script automates the process of logging in, navigating through pages, extracting data from tables, and storing the data into a Google Spreadsheet.
